main
  local
    integer v1;
  do
    
	v1 = 10;

	while (v1 > 0) // 3.3
	do
	  
	  if (v1 > 5) // 3.2
	  then 
	  do 
	    write(1); // 3.4
	  end
	else
	  do
	    write(2); // 3.4
	  end;
	  
	  v1 = v1 - 1;
	end;
	
	write (v1); // 3.4
	
  end
