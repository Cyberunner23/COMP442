class C1 {
	public float a;
	public integer b[5];
	public integer c;
};

class C2 {
	public C1 a;
	public integer b;
	
	//public hello(integer c) : integer;
};

/*C2::hello(integer c) : integer
  local
    C1 c1;
    integer hey;
  do
    //hey = ((30 * -2) / not 2) / (5 + (5*5));
  end;

squared(integer a) : integer
  local
    integer result;
  do
    //result = a * a;
	return (a);
  end;*/

main
  local
    //integer val[3][2];
	//integer arr[2];
	integer a;
  do

    //val[1 + 0][0] = 5;
	while (0 > 1)
	  do
	  end;
      
	
    //val = 5 - 3;
	//val = 7 / 8 * 9;
	//val = val * val;
	//result = squared(val);
	//write (result);
  end